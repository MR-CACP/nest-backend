# 代码注释风格指南（Comment Style Guide）

> 适用于 `src/` 下全部代码；`test/` 只需在关键断言与复杂夹具处遵循；一次性脚本/临时文件不强制。
> 目标是让注释看起来出自同一人之手，并且让第一次接触代码的新手能独立读懂每个模块。

---

## 1. 三条核心原则

1. **解释“为什么”，不翻译“是什么”。** 注释是补代码表达不了的信息：设计取舍、边界条件、失败语义、坑。逐行翻译代码的注释是噪音。
2. **注释写给下一个维护者——很可能就是未来的你。** 三个月后你需要靠注释回忆当时的决策。
3. **冗余注释比没有注释更糟。** 过时、重复、显而易见的注释会误导人。发现就删。

改代码必须同步改注释。注释和代码一起评审、一起提交。

---

## 2. 格式标准

### 2.1 单行注释 `//`

- 行内简短说明，或连续多行组成的段落说明（段注释每行都以 `//` 开头）。
- 行尾注释仅在**一行能放下**时使用，与代码之间空两个空格。
- 长段逻辑、决策背景放在代码块**之前**，不要夹在代码中间。

```ts
// 空串按“无鉴权”处理（ioredis 会把空密码当作 AUTH 参数发送）
password: cfg.password || undefined,

// TTL 非法属编程错误：必须先于序列化校验（若先序列化，循环引用 + 非法 TTL
// 会先被序列化分支吞掉，非法 TTL 反而不抛出，违背方法级契约）
if (ttlSeconds !== undefined) {
  this.assertValidTtl(ttlSeconds);
}
```

### 2.2 函数/方法注释（JSDoc）`/** ... */`

所有**导出的公共函数/方法**必须有；私有小函数可用一行 `/** ... */`。

结构固定：

```ts
/**
 * 一句话功能说明（动词开头，说明做什么 + 关键边界）。
 * 第二行起补充“为什么这么写”或失败语义（可选，有才写）。
 *
 * @param paramName 参数含义与约束（单位、允许范围、特殊值语义）
 * @returns 返回值含义；无返回值（void）不写
 * @example 调用示例（仅当调用约定不直观时使用）
 */
```

要求：

- 类型由 TypeScript 承担，**不写 `@type` / 类型名**；`@param` 只写“含义 + 约束”。
- 单位必须标注（毫秒/秒），特殊值必须说明（`0=禁用`、`null=未命中`）。
- `@returns` 写正常值；若有异常/降级分支，在正文或 `@returns` 中说明。
- `@example` 按需：调用方式一眼能看懂的（`service.get(key)`）不写。

```ts
/**
 * 客户端查询超时（毫秒，0=禁用）：兜底中止在途查询（pg 到达后仅本地回调失败，不取消服务端查询）。
 * @param env 环境变量对象；缺省读 process.env
 */
```

### 2.3 类/接口/类型注释

- **类**：顶部 `/** 职责 + 设计要点（为什么这么设计、取舍） */`。模板风格见 `RedisService`。
- **接口**：顶部一行用途说明；每个字段上方一行 `/** 含义（含单位/边界） */`。
- **type 别名**：`/** 取值范围与语义 */`，即使只有一行。

```ts
/** TypeORM 查询日志选项：false 关闭 / 'all' 全开 / 按级别数组（query,schema,error,...） */
export type DbLoggingOption = LoggerOptions;

export interface CorsConfig {
  /** 允许的来源；'*' 表示任意来源（保留字符串语义，交给 cors 库处理） */
  origins: string | string[];
  credentials: boolean;
}
```

### 2.4 常量注释

- 导出常量必须说明**含义、单位、边界**。
- 魔法数字/魔法字符串一律解释；注释尽量写在常量声明处，而不是使用处。

```ts
/** PostgreSQL 毫秒型 GUC 上界（int32 max），超界值会被 PostgreSQL 以 out of range 拒绝 */
export const PG_MS_GUC_MAX = 2_147_483_647;
```

### 2.5 TODO / FIXME

- `// TODO(作者): 为什么现在不做、打算怎么做` —— 待办。
- `// FIXME(作者): 已知缺陷及其影响` —— 已知问题。
- 两者都可被 `grep` 检索，因此**格式必须统一**，不允许自由发挥。

### 2.6 分区注释

长文件（>150 行）可用 `// ---------- 标题 ----------` 分区（`app.setup.ts` 已用此惯例）。小文件不要用，避免噪音。

---

## 3. 内容规范（新手友好）

1. **用通俗简体中文**；必须用术语时首次出现给一句简短解释。
   - ✅ `ioredis 对 0 会执行 setTimeout(..., 0) 导致每条命令立即超时`
   - ❌ `经 Redis 协议栈的命令超时机制在零值下的非预期语义`
2. **写清边界与失败语义**：什么情况下返回 `null`、抛出异常、静默降级、跳过写入。
3. **单位与范围必须标注**：`毫秒`/`秒`、`0=禁用`、`max` 上界、`仅限生产`。
4. **引用代码文件用相对路径**：`common/utils/request-id.ts`（不带 `src/` 前缀按项目惯例书写，但全文保持一致）。
5. 注释和代码一样遵守 lint：行宽、格式由 Prettier 统一，注释写完后必须通过 `pnpm lint:check`。

---

## 4. 禁止清单（写了就删）

| 类型 | 例子 |
|------|------|
| 复述代码 | `// 设置密码` 后跟 `password = 'x'` |
| 逐行翻译 | `// 循环遍历数组` 后跟 `for (...) {...}` |
| 显而易见 | `// 返回 true 表示成功` |
| 过时注释 | 代码已改、注释没改（比没有注释更危险） |
| 个人备注/历史 | `// 上次部署这里出了问题`（放 commit message / 文档） |
| 大段英文 | 非技术名词的英文句子（技术名词如 Joi、pg、GUC 保留原文） |

---

## 5. 好/坏对照示例

**坏：复述代码**

```ts
// 从环境变量读取数据库主机
const host = env.DB_HOST;
```

**好：补充代码没有的信息**

```ts
// dotenv 默认不覆盖 process.env 中已有的变量，因此容器注入的变量始终最高
const host = env.DB_HOST;
```

---

**坏：没有边界说明的参数**

```ts
/** @param ttlSeconds TTL */
async set(key: string, value: unknown, ttlSeconds?: number): Promise<void>
```

**好：边界与失败语义完整**

```ts
/**
 * 写入缓存；序列化或 Redis 故障时记录并跳过，不让业务请求 500。
 * @param ttlSeconds 过期秒数；省略则永不过期（务必显式给过期时间，防止无界增长）。
 *                  非法值（≤0 或非整数）属编程错误，直接抛出 RangeError
 */
async set(key: string, value: unknown, ttlSeconds?: number): Promise<void>
```

---

**坏：无注释的类**

```ts
export class RedisService implements OnModuleDestroy {
  async get(key) { ... }
}
```

**好：职责 + 失败语义一读即懂**

```ts
/**
 * JSON 序列化的缓存门面：业务代码不直接接触 ioredis，统一键前缀由客户端 keyPrefix 承担。
 * 失败语义：get/set/del/ttl 缓存侧故障一律记录后降级，不让业务请求 500；
 * ping 透传真实状态供健康检查；TTL 非法属编程错误直接抛出。
 */
@Injectable()
export class RedisService implements OnModuleDestroy
```

---

## 6. 项目约定速查

| 项 | 约定 |
|----|------|
| 语言 | 简体中文；Joi/pg/ioredis/GUC 等技术名词保留英文 |
| 标点 | 全角中文标点；代码标识符、路径、命令用反引号包裹 |
| 动词开头 | 读取、写入、校验、拒绝、降级、跳过、抛出 |
| 失败语义用词 | 统一：`降级`（不 500）、`拒绝`（校验失败）、`跳过`（非致命跳过）、`抛出`（编程错误） |
| JSDoc 标签 | 只用 `@param` / `@returns` / `@example`；不写 `@type` |
| 单位 | 毫秒/秒必须标注；`0=禁用` 等特殊值必须说明 |
| 文件引用 | 相对路径，与全文一致 |

---

## 7. 写注释前的检查清单（评审时逐项核对）

1. [ ] 这条注释是否回答了“为什么”？——答不出就删。
2. [ ] 代码改动是否同步更新了注释？
3. [ ] 公共函数是否都有 JSDoc，`@param`/`@returns` 是否完整且未过时？
4. [ ] 边界语义（null/异常/降级/禁用值）是否说清？
5. [ ] 是否有禁止清单中的冗余注释？
6. [ ] 是否有非技术性的英文残留？
7. [ ] 注释长度是否克制？超过 5 行说明应该拆成段注释或挪进设计文档。
8. [ ] 新 API 第一次被其他人用时，能否只靠注释+签名就正确调用？

---

## 8. 自查命令（PowerShell）

```powershell
# 查 TODO/FIXME（必须带作者与说明）
Get-ChildItem src -Recurse -Filter *.ts | Select-String -Pattern 'TODO|FIXME'

# 查疑似复述代码的短注释（抽查）
Get-ChildItem src -Recurse -Filter *.ts | Select-String -Pattern '^\s*//\s*(设置|返回|循环|遍历)'
```

正式提交前请人工跑一遍第 7 节清单；CI 不拦截注释质量，靠约定与评审保证。
